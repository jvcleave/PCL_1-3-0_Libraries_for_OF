REQUIREMENTS
libusb installed via MacPorts or OpenNI (needs to reside in opt/local/include and opt/local/lib)

Develop Branch is using OpenNI installed on the system
Contents of "COPY_CONTENTS_TO_etc" needs to be copied into /etc

openFrameworks 007